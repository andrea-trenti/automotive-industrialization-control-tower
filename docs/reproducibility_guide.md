# Reproducibility Guide

Run `python pipeline.py` then `pytest -q`. All key final numbers are stored in outputs/core_results.json and checked against README. Release artifacts are produced from one source tree; GITHUB is derived from FULL by excluding large regenerable raw data only.
