# Project Network Methodology

Activities form a DAG. CPM produces earliest dates under infinite-resource deterministic durations. This is explicitly a baseline, not a launch forecast. Near-criticality and stochastic criticality are treated separately because path membership changes under uncertainty.
